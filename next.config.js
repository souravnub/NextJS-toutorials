/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["www.svgrepo.com", "images.unsplash.com"],
    },
};

module.exports = nextConfig;
