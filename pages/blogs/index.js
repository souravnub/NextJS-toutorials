import React from "react";

const BlogsPage = () => {
    // this page has no defined pre rendering method... but nextjs will by-default pre-render each and every page with SSG until-n-unless a pre-rendering method is not manually supplied
    return (
        <div>
            BlogsPage
            <h1>blogs page hahahahha</h1>
        </div>
    );
};

export default BlogsPage;
